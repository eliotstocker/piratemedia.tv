{
  "name": "piratemedia.tv",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "npm run build && npx http-server",
    "build": "npm run build:fs",
    "build:fs": "../sh.js/bin/exportFileSystem.js ./fs -v fs -o fs.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ansi-256-colors": "^1.1.0",
    "marked": "^0.7.0",
    "marked-terminal": "^3.3.0",
    "png.js": "^0.2.1"
  },
  "devDependencies": {
    "brsh": "1.0.0"
  }
}
